# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1

# --- LangChain ---
langchain==0.3.13
langchain-core==0.3.28
langchain-text-splitters==0.3.4

# --- LLM + Embeddings (both via Google Gemini) ---
#
# NOTE: sentence-transformers and langchain-huggingface were REMOVED here.
# They depend on PyTorch (~250-400MB RSS on import), which OOMs a 512MB host
# before a single chunk is embedded. Embeddings now run through the Gemini
# API, so no model weights are held in memory.
langchain-google-genai==2.0.7

# --- Vector database ---
langchain-chroma==0.1.4
chromadb==0.5.23

# --- Git ---
GitPython==3.1.43
